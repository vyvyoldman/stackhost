```yaml
# StackHost configuration file
# Version: 2.0.0

user:
  name: string
  github: string

runtime:
  image: node:22

commands:
  package: ""
  build:
    - npm install
  start: node index.js

repository:
  branch: main
  auto_deploy: true

env:
  - NODE_ENV:production
```
